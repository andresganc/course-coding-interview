
import cleanArray from '../src/index'

describe('CLEAN ARRAY ', () => {
    
    test('Should clean the array of null, undefinend, false', () => {
        
        
        // const value = 2 + 2
        //expect(value).toBe(4)

        //expect(cleanArray([1, null, 5, 78, false])).toContain([1,5, 78])

    })
    

})
